# SkillLink2 (Llinks) — обмен навыками 1-на-1

Мобильное iOS-приложение, где пользователи находят друг друга по навыкам:  
**я могу обучать** ↔ **я хочу изучить**.  
Есть рекомендации, лайки/матчи и чат.

---

## 🔥 Основные фичи
- Авторизация через Firebase (Google Sign-In)
- Профиль пользователя: имя/фамилия, город, био, навыки (учу / изучаю)
- Рекомендации (Discover) с matchScore + объяснение “почему рекомендован”
- Знакомства (Swipe) — лайк/пропуск
- Матч → создаётся чат
- Чат с сообщениями в реальном времени (Firestore)

---

## 🧱 Стек
- SwiftUI
- Firebase Auth
- Cloud Firestore
- (опционально) Firebase Storage / Push-уведомления — в планах

---

## 📦 Структура данных Firestore (основное)

### users/{uid}
Профиль пользователя:
- `fullName`, `firstName`, `lastName`
- `location`, `bio`
- `teachSkills: [String]`
- `learnSkills: [String]`
- `profileCompleted: Bool`
- `rating`, `points`
- `createdAt`, `updatedAt`

Подколлекции:
- `users/{uid}/likes/{otherUid}`
- `users/{uid}/passes/{otherUid}`

### matches/{pairId}
- `users: [uid1, uid2]`
- `createdAt`

### chats/{pairId}
- `users: [uid1, uid2]`
- `createdAt`
- `lastMessage`, `lastMessageAt`, `lastSenderId`

### chats/{pairId}/messages/{messageId}
- `senderId`
- `text`
- `createdAt`

---

## 🚀 Запуск проекта

### 1) Требования
- Xcode (желательно последняя версия)
- iOS 16+ (или как настроено в проекте)
- Аккаунт Firebase

### 2) Firebase настройка
1. Создай проект в Firebase Console
2. Добавь iOS App (Bundle ID должен совпадать с проектом)
3. Скачай `GoogleService-Info.plist`
4. Добавь его в Xcode в Target Membership приложения

### 3) Firestore Rules
В Firestore → Rules вставь rules (см. `firestore.rules` или раздел ниже).  
Проверь, что чтение чатов/матчей разрешено **только участникам**.

### 4) Индексы (если потребуется)
Если Firestore ругается “The query requires an index” — создай индекс по ссылке из лога.
Обычно нужны:
- `users: profileCompleted ASC, updatedAt DESC`
- `sessions` (если включены занятия)

---

## 🔐 Безопасность (коротко)
- Профили видны только авторизованным
- Лайки/пассы — приватные подколлекции пользователя
- Чаты/матчи читают только участники (arrayContains uid)
- Сообщение может отправить только участник чата и только от своего senderId

---

## 🧪 Как тестировать быстро
1. Запусти на симуляторе/айфоне под аккаунтом A
2. Заполни профиль и навыки
3. Выйди, зайди под аккаунтом B
4. Сделай взаимный лайк → должен появиться чат
5. Напиши сообщения с двух аккаунтов

---

## 🛣 Roadmap (что дальше)
- Нормальная система “Занятие” внутри чата (создать/подтвердить/начать/завершить)
- Уведомления о занятии (за 30 минут и в момент старта)
- Рейтинг после занятия + начисление баллов
- Улучшение рекомендаций (веса навыков, фильтры, анти-спам)

---

## 👤 Автор
Rodion (has1l)
